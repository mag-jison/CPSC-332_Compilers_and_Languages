! If-While Statements !

begin
	if x > y then
		if y > z then
			max = x;
		else
			max = y;
		endif
	else
		max = z;	
	endif;

	while i < arr_length do 
		if curr > max then
			curr = max + x;
		else
			curr = max - y;
		endif
	whileend
end
